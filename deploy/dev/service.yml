apiVersion: v1
kind: Service
metadata:
  name: eda-services-log-summarization-svc
  namespace: eda-services
  labels:
    app.kubernetes.io/name: eda-services-log-summarization-deploy
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: eda-services-log-summarization-deploy
  ports:
    - name: svcedas-port
      port: 8080
      targetPort: es-app-port
      protocol: TCP
    - name: svcedas-sslport
      port: 443
      protocol: TCP
      targetPort: es-app-port